---
 - name: pulling vault through jenkins
   hosts: localhost
   become: true
   gather_facts: false
   vars_files:
     - Alc_Ansi_project/playbook/vault.yml
   tasks:
     - name: Show password from vault
       debug:
         msg: "The password is {{ password }}"
...
