---
- name: Install latest version for all the servers
  hosts: localhost 
  become: true
  tasks:
    - name: Ensure latest version of httpd is installed
      yum:
        name: httpd
        state: latest

    - name: Ensure httpd service is started and enabled
      service:
        name: httpd
        state: started
        enabled: yes
...